function soma(x = 1, y = 1) { //Fazendo isso você faz com que a função assuma um resultado caso você não coloque nada
    const resultado = x + y;
    return resultado;
}

//console.log(soma(2 , 2));
//console.log(soma(6 , 8));
//console.log(soma(5 , 9));


//console.log(resultado); // Não irá aparecer pois o valor esta protegido pelo escopo da função

const resultado = soma(4, 2);
console.log(resultado);

//Declaração classica de funçãovb vbb bbc v m