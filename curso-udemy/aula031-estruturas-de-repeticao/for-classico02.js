const frutas = ['Maçã', 'Pêra', 'Uva', 'Abacate', 'Manga', 'Jaca', 'Laranja', 'Melancia'];

//console.log(frutas.length); // ira falar o tamanho do array

for (let i = 0; i < frutas.length; i ++) {
   console.log(`Índice ${i}`, frutas[i]);
}