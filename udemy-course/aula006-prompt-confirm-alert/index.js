let num1 = Number(prompt('Digite um número'));
let num2 = Number(prompt('Digite outro número'));

const resultado = num1 + num2;

const alerta = alert(`O resultado da soma dos dois números é: ${resultado}.`);

confirm(`O resultado do valor: ${resultado} está correto?`);